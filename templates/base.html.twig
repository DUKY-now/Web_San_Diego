<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{ title|default('Project San Diego') }}</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
    <header>
        <h1>Project San Diego</h1>
        <nav>
            <a href="{{ path('app_home_page') }}">Accueil</a>
            <a href="{{ path('app_faction') }}">Factions</a>
            <a href="{{ path('app_reglement') }}">Règlement</a>
            <a href="{{ path('app_equipe_moderation') }}">Équipe</a>
        </nav>
    </header>

    <main>
        {% block body %}{% endblock %}
    </main>

    <footer>
        <p>&copy; {{ "now"|date("Y") }} Project San Diego</p>
    </footer>
</body>
</html>
