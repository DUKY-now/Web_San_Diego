{% extends 'base.html.twig' %}

{% block title %}Ticket - Project San Diego{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<h1>Liste des tickets</h1>

<a href="{{ path('app_ticket_new') }}">Créer un ticket</a>

<ul>
    {% for ticket in tickets %}
        <li>{{ ticket.titre }} - {{ ticket.description }}</li>
    {% else %}
        <li>Aucun ticket pour le moment.</li>
    {% endfor %}
</ul>

</div>
{% endblock %}
