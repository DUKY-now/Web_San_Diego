{% extends 'base.html.twig' %}

{% block title %}Factions - Project San Diego{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('css/faction.css?v=4') }}">
{% endblock %}
{% block body %}
<h1>Factions disponibles</h1>

<div class="cards">
    {% for faction in factions %}
        <div class="card">
            <img src="{{ asset('images/Factions/' ~ faction.slug ~ '.png?v=2') }}" alt="{{ faction.nom }}" class="faction-logo">
            <h2>{{ faction.nom }}</h2>
            <p>{{ faction.lore |slice(0, 100) ~ '...' }}</p>
            <p><strong>Membres:</strong> {{ faction.membres }}</p>
            <a href="{{ path('app_faction_show', {'slug': faction.slug}) }}">Voir plus</a>
        </div>
    {% endfor %}
</div>
{% endblock %}

